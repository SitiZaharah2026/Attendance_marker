Create a reusable bilingual language switcher for CelikSense AI.

Requirements:
- English and Bahasa Melayu support.
- Update headings, descriptions, labels, buttons, and audio instruction text.
- Use JavaScript object dictionary:
  const translations = { en: {...}, bm: {...} }
- Save selected language in localStorage.
- Apply chosen language when page loads.
- Include example for homepage, dashboard, ADHD Agent, and Dyslexia Agent.
